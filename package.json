{
  "private": true,
  "name": "product-kit",
  "version": "0.5.1",
  "scripts": {
    "dev": "next -p $(./node_modules/detect-port/bin/detect-port 3000)",
    "build": "next build",
    "start": "next start",
    "deploy:gh": "IS_GH=true next build && IS_GH=true next export -o dist && gh-pages -d dist -t",
    "deploy:preview": "now",
    "now-build": "next build && next export -o dist"
  },
  "dependencies": {
    "@emotion/core": "^10.0.10",
    "@emotion/styled": "^10.0.11",
    "emotion-theming": "^10.0.10",
    "next": "^8.1.0",
    "now": "^15.3.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "styled-system": "^5.0.2",
    "webpack": "^4.32.2"
  },
  "devDependencies": {
    "babel-plugin-emotion": "^10.0.9",
    "detect-port": "1.3.0",
    "fs-extra": "^8.0.1",
    "gh-pages": "^2.0.1"
  }
}
